using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Purchasing;
// using Google.Play.Billing;

public class PurchaseManager : MonoBehaviour
{
    private void Awake()
    {
        // Create a builder using the GooglePlayStoreModule.
        /*var configurationBuilder = ConfigurationBuilder.Instance(Google.Play.Billing.GooglePlayStoreModule.Instance());
        
        ConfigurationBuilder builder;
        if (Application.platform == RuntimePlatform.Android /*&& SelectedAndoidAppStore == AppStore.GooglePlay#1#)
        {
            builder = ConfigurationBuilder.Instance(Google.Play.Billing.GooglePlayStoreModule.Instance());
        }
        else
        {
            builder = ConfigurationBuilder.Instance(StandardPurchasingModule.Instance());
        }*/
    }
}
